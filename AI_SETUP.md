# ربط AI والجدولة الحقيقية

هذه النسخة تضيف Netlify Functions:
- `/api/ai-chat` للمحادثة
- Scheduled Function يومياً لتوليد التوكيد/التمرين/السلوك
- Scheduled Function أسبوعياً لتوليد المقال

## 1) مفتاح AI
في Netlify: Site configuration → Environment variables أضيفي:
`GEMINI_API_KEY`

لا تضعي المفتاح داخل `app.js` أو HTML. توثيق Netlify يوضح أن متغيرات البيئة متاحة للـFunctions ويمكن استخدامها لحفظ مفاتيح API، وتوثيق Google يوصي باستخدام مفاتيح/متغيرات بيئة بدلاً من كشفها في المتصفح.

## 2) قاعدة البيانات
الوظائف المجدولة في هذه النسخة تنتج المحتوى. لكي يُحفظ ويظهر تلقائياً لجميع المستخدمات، اربطي `CONTENT_WEBHOOK_URL` بخدمة Backend/قاعدة بيانات. لا نستخدم localStorage كقاعدة بيانات عامة.

## 3) حساب Admin
النسخة الحالية لا تجعل `admin.html` نظام دخول آمناً. عند ربط Backend نحتاج مصادقة حقيقية للمؤسسة والمستخدمات، مع كلمة المرور مخزنة في نظام مصادقة آمن.

## 4) النشر
ارفعي هذه النسخة إلى Netlify كمشروع جديد أو اربطيها بمستودع Git. يجب أن تُنشر `netlify/functions` مع المشروع حتى تعمل الوظائف.

## 5) الملاحظة المالية
Netlify تقول إن Scheduled Functions متاحة على جميع خططها، لكن تشغيل نموذج AI قد يخضع لحدود/حصص مزود AI. Google توضح أن الاستخدام المدفوع يرفع الحدود ويتطلب إعداد الفوترة. لذلك ابدئي بالحد المجاني وراقبي الاستخدام.
